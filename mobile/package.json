{
  "name": "bt-secretary-app",
  "version": "0.1.0",
  "description": "BT Secretary — application mobile terrain (iOS + Android) pour saisie temps, bons de travail et factures photo. Wrapper HTML5 de comptaria.ca via Capacitor. Intégration mcp-comtaria + mcp-csv + mcp-acomba. Souveraineté Québec + Loi 25.",
  "private": true,
  "scripts": {
    "build:web": "echo 'site web build step (vite/copy, voir ../package.json)'",
    "sync:web": "../node_modules/.bin/cap sync web",
    "sync:android": "../node_modules/.bin/cap sync android",
    "sync:ios": "../node_modules/.bin/cap sync ios",
    "open:android": "../node_modules/.bin/cap open android",
    "open:ios": "../node_modules/.bin/cap open ios",
    "lint": "echo 'no app-side lint yet (web side linted at root)'",
    "test": "echo 'no app-side tests yet'"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/camera": "^6.1.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/network": "^6.0.0",
    "@capacitor/preferences": "^6.0.0",
    "@capacitor/share": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
